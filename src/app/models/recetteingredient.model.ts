export interface RecetteIngredient {
    ingredientId: string; // ID de l'ingrédient dans la base
    nom: string;          // Nom de l'ingrédient
    quantite: number;     // Quantité utilisée dans la recette
    unite: string;        // Unité de mesure
 }  