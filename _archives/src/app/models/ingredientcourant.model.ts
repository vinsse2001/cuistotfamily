export interface IngredientCourant {
    id: string;          // Identifiant de l'ingrédient dans ingredients.json
    nom: string;          // Nom complet de l'ingrédient
    nomSimplifie: string; // Nom simplifié défini par l'utilisateur
  }
  